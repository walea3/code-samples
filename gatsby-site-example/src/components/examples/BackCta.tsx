import { TextButton } from '@nel-ui/react';
import React from 'react';

const BackCtaExample = ({ text }) => (
  <TextButton icon="sys-left" iconLeft>
    {text}
  </TextButton>
);

export default BackCtaExample;
